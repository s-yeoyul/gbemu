# gbemu

Gameboy Emulator Developed in C++20.

## Build
```bash
cmake -S . -B build
cmake --build build -j
```

## Run
```bash
cd build/
./gbemu
```

## Notes
ROM / Boot ROM are not included in this project.

