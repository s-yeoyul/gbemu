# gbemu

Gameboy Emulator Developed in C++20.

## Build
```bash
cmake -S . -B build
cmake --build build -j
```
Simply, you can execute `./run.sh`.

## Run
```bash
./bulid/gbemu
```
Do not execute binary in `build/` directroy. 
